RewriteEngine On

# 禁止直接访问敏感文件
<FilesMatch "config\.php$">
  Require all denied
</FilesMatch>

# 禁止访问 db/ 目录
RewriteRule ^db/ - [F,L]

# 禁止访问 temp/ 目录（通过 image.php 代理访问）
RewriteRule ^temp/ - [F,L]

# 允许 API 访问
RewriteRule ^api/ - [L]
